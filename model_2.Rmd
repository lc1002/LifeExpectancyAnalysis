---
title: "model_2"
author: "AnMei Chen"
date: "1/7/2022"
output: html_document
---

```{r}
library(tidyverse)
library(MASS)
library(performance) # vif
library(caret)
library(car)
library(Metrics)
```

```{r}
le_df = 
  read_csv("./data/Complete_Data.CSV") %>% 
  rename("HDI" = "incomecompositionofresources", "percent_exp" = "percentageexpenditure", "total_exp" = "totalexpenditure", "le" = "lifeexpectancy", "adult_mort" = "adult_mortality", "thin_1_19" = "thinness1_19years", "thin_5_9" = "thinness5_9years", "under_five" = "under_fivedeaths") 
```

## model 2

```{r}
model_2 =
  lm(le ~ status + bmi + population + thin_1_19 + thin_5_9 + status * bmi + status * thin_1_19 + status * thin_5_9, data = le_df)
summary(model_2)
```

## Use diagnostic plots to check model 2

```{r}
par(mfrow = c(2, 2))
par(mar = c(2,2,2,2))
#Residuals vs Fitted values Plot
plot(model_2, which = 1)
# QQ Plot
plot(model_2, which = 2)
# Scale-location plot
plot(model_2, which = 3)
# Residuals vs. Leverage
plot(model_2, which = 5)
par(mfrow = c(1, 1)) 
par(mar = c(3,3,3,3))
```

## Box-cox Transformation

```{r}
bc = boxcox(model_2)
lambda = bc$x[which.max(bc$y)]
lambda

le_df =
  le_df %>% 
  mutate(bc_le = (le)^2)

model_2_bc = lm(bc_le ~ status + bmi + population + thin_1_19 + thin_5_9 + status * bmi + status * thin_1_19 + status * thin_5_9, data = le_df)
summary(model_2_bc)

model_2 = lm(le ~ status + bmi + population + thin_1_19 + thin_5_9 + status * bmi + status * thin_1_19 + status * thin_5_9, data = le_df)
summary(model_2)

# compare diagnostic plots
par(mfrow = c(2, 1))
par(mar = c(2,2,2,2))

plot(model_2_bc, which = 1)  
plot(model_2, which = 1)

plot(model_2_bc, which = 2)  
plot(model_2, which = 2)

plot(model_2_bc, which = 3)  
plot(model_2, which = 3)

plot(model_2_bc, which = 5)  
plot(model_2, which = 5)

par(mfrow = c(1, 1)) 
par(mar = c(3,3,3,3))

```

## Identify influentical observations

```{r}
plot(model_2_bc, which = 5)

# Identify influential observations using Cook's distance
cutoff <- 4/nrow(le_df)
plot(model_2_bc, which = 4, cook.levels = cutoff)
abline(h = cutoff, lty = 2, col = "red")

cook = cooks.distance(model_2_bc)
influential_obs = as.numeric(names(cook)[(cook > cutoff)])

#define new data frame with influential points removed
no_influ_le = le_df[-influential_obs, ]

no_influ_model_2_bc = lm(bc_le ~ status + bmi + population + thin_1_19 + thin_5_9 + status * bmi + status * thin_1_19 + status * thin_5_9, data = no_influ_le)
summary(no_influ_model_2_bc)
```

## Leverage checking 

```{r}
# compare models with and without influential observations

no_influ_model_2_bc = lm(bc_le ~ status + bmi + population + thin_1_19 + thin_5_9 + status * bmi + status * thin_1_19 + status * thin_5_9, data = no_influ_le)
summary(no_influ_model_2_bc)

model_2_bc = lm(bc_le ~ status + bmi + population + thin_1_19 + thin_5_9 + status * bmi + status * thin_1_19 + status * thin_5_9, data = le_df)
summary(model_2_bc)

```

## Multi-collinearity

```{r}
check_collinearity(no_influ_model_2_bc)
```

## Model Validation

```{r}
set.seed(1)

# Use 5-fold validation and create the training sets
train = trainControl(method = "cv", number = 5)

# Fit the 4-variables model
model = train(bc_le ~ status + bmi + population + thin_1_19 + thin_5_9 + status * bmi + status * thin_1_19 + status * thin_5_9, data = no_influ_le, trControl = train, method = 'lm', na.action = na.pass)

model$finalModel
print(model)
```

## normalized RMSE

```{r}
rmse = model$results$RMSE / (max(no_influ_le$bc_le) - min(no_influ_le$bc_le))
rmse
```

## Added variable plot

```{r}
avPlots(no_influ_model_2_bc)
```
